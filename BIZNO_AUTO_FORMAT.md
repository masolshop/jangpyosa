# 사업자등록번호 자동 포맷팅 개선

## 📋 개요

장애인직원 회원가입 페이지(`/employee/signup`)에서 사업자등록번호 입력 시 자동으로 하이픈이 추가되도록 개선했습니다.

## 🎯 문제점

### 이전 상황
- 사용자가 하이픈 없이 숫자만 입력하는 경우가 많음
- 플레이스홀더에 "123-45-67890 또는 1234567890 입력 가능"으로 표시
- 백엔드에서는 하이픈 유무와 관계없이 매칭되지만, 사용자가 혼란스러워함

## ✅ 해결 방법

### 자동 포맷팅 함수 추가

```typescript
// 사업자등록번호 자동 포맷팅 (123-45-67890)
const formatBizNo = (value: string): string => {
  const numbers = value.replace(/\D/g, ''); // 숫자만 추출
  
  if (numbers.length <= 3) {
    return numbers;
  } else if (numbers.length <= 5) {
    return `${numbers.slice(0, 3)}-${numbers.slice(3)}`;
  } else if (numbers.length <= 10) {
    return `${numbers.slice(0, 3)}-${numbers.slice(3, 5)}-${numbers.slice(5)}`;
  }
  
  return `${numbers.slice(0, 3)}-${numbers.slice(3, 5)}-${numbers.slice(5, 10)}`;
};
```

### 입력 핸들러 개선

```typescript
onChange={(e) => {
  const formatted = formatBizNo(e.target.value);
  setBizNo(formatted);
}}
```

### UI 개선

**이전:**
```
입력값: 1234567890
표시: 1234567890
플레이스홀더: 123-45-67890 또는 1234567890 입력 가능
도움말: 하이픈은 자동으로 제거됩니다
```

**이후:**
```
입력값: 1234567890 (사용자가 입력한 값)
표시: 123-45-67890 (자동으로 포맷팅)
플레이스홀더: 123-45-67890
도움말: (제거됨)
```

## 🎨 사용자 경험 개선

### 입력 과정
1. **처음 3자리**: `123`
2. **4-5자리**: `123-45`
3. **6-10자리**: `123-45-67890`
4. **10자리 초과**: 자동으로 10자리까지만 유지

### 백엔드 처리
- 백엔드 API에서는 하이픈을 자동으로 제거하여 처리
- 사용자가 하이픈을 입력하든, 안 하든 정상 매칭됨

```typescript
// apps/api/src/routes/auth.ts
const cleanedBizNo = bizNo.replace(/\D/g, ''); // 숫자만 추출
```

## 📊 테스트 케이스

### Case 1: 하이픈 없이 입력
```
입력: 1234567890
결과: 123-45-67890 (자동 포맷팅)
백엔드 처리: 1234567890 (하이픈 제거)
매칭: ✅ 성공
```

### Case 2: 하이픈과 함께 입력
```
입력: 123-45-67890
결과: 123-45-67890 (유지)
백엔드 처리: 1234567890 (하이픈 제거)
매칭: ✅ 성공
```

### Case 3: 불규칙하게 입력
```
입력: 123-4-567890
결과: 123-45-67890 (자동 재포맷팅)
백엔드 처리: 1234567890 (하이픈 제거)
매칭: ✅ 성공
```

## 🔧 수정 파일

### 프론트엔드
- `apps/web/src/app/employee/signup/page.tsx`
  - `formatBizNo` 함수 추가
  - 입력 핸들러에 자동 포맷팅 적용
  - 플레이스홀더 단순화
  - 불필요한 도움말 제거

## 🌐 배포 정보

- **커밋**: `bf374d7`
- **날짜**: 2026-03-05
- **브랜치**: `main`

### 배포 단계
1. ✅ 코드 수정 완료
2. ✅ Git 커밋 및 푸시
3. ✅ 프로덕션 서버 배포
4. ✅ Next.js 빌드 (v14.2.35)
5. ✅ PM2 재시작 완료
6. ✅ 서비스 정상 작동

## 📱 테스트 방법

### 1. 장애인직원 회원가입 페이지 접속
```
URL: https://jangpyosa.com/employee/signup
```

### 2. 사업자등록번호 입력 테스트
- 숫자만 입력: `1234567890`
- 자동으로 `123-45-67890` 형식으로 변환됨

### 3. 회사 정보 확인
- "확인" 버튼 클릭
- 등록된 회사 정보가 표시됨

## 📈 기대 효과

1. **사용자 경험 개선**
   - 하이픈 입력 여부에 대한 고민 제거
   - 일관된 입력 형식 제공

2. **입력 오류 감소**
   - 자동 포맷팅으로 입력 실수 방지
   - 시각적으로 확인 가능한 형식

3. **매칭 성공률 향상**
   - 백엔드에서 하이픈 제거 처리
   - 입력 방식과 관계없이 정확한 매칭

## 🔄 향후 개선 사항

1. **다른 페이지에도 적용**
   - 일반 회원가입 페이지 (`/signup`)
   - 관리자 페이지의 사업자등록번호 입력 필드

2. **추가 유효성 검사**
   - 사업자등록번호 체크섬 검증
   - 실시간 유효성 피드백

## ✅ 검증 완료

- [x] 프론트엔드 자동 포맷팅 구현
- [x] 백엔드 하이픈 제거 처리 확인
- [x] 프로덕션 환경 배포 완료
- [x] 실제 사용자 시나리오 테스트
- [x] 문서화 완료

---

**작성일**: 2026-03-05  
**작성자**: AI Assistant  
**관련 커밋**: bf374d7
