# 회사 공지사항 시스템 구현 완료

## 📋 구현 일시
- **날짜**: 2026년 2월 20일
- **Git Commit**: 38915fa (API 라우트 Express 수정), 이전 커밋 포함

## 🎯 구현 목표
바이어 직원이 회사 전체 장애인 직원에게 회사 공지를 하고, 직원이 출퇴근 페이지에서 공지를 확인한 후 읽음 확인 버튼을 클릭하면 바이어 대시보드에서 읽은 직원 리스트를 확인할 수 있는 시스템 구축

## ✅ 구현 기능

### 1. 데이터베이스 스키마 추가

#### CompanyAnnouncement 모델
```prisma
model CompanyAnnouncement {
  id            String                   @id @default(cuid())
  companyId     String
  buyerId       String
  company       Company                  @relation(fields: [companyId], references: [id], onDelete: Cascade)
  buyerProfile  BuyerProfile             @relation(fields: [buyerId], references: [id], onDelete: Cascade)
  
  title         String                   // 공지 제목
  content       String                   // 공지 내용
  priority      String                   @default("NORMAL") // URGENT, NORMAL, LOW
  isActive      Boolean                  @default(true)
  
  createdById   String                   // 작성자 User ID
  createdBy     User                     @relation(fields: [createdById], references: [id])
  
  readLogs      AnnouncementReadLog[]    // 읽음 기록
  
  createdAt     DateTime                 @default(now())
  updatedAt     DateTime                 @updatedAt
  
  @@index([buyerId])
  @@index([companyId])
}
```

#### AnnouncementReadLog 모델
```prisma
model AnnouncementReadLog {
  id              String               @id @default(cuid())
  announcementId  String
  employeeId      String
  userId          String
  
  announcement    CompanyAnnouncement  @relation(fields: [announcementId], references: [id], onDelete: Cascade)
  employee        DisabledEmployee     @relation(fields: [employeeId], references: [id], onDelete: Cascade)
  user            User                 @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  readAt          DateTime             @default(now())
  
  @@unique([announcementId, employeeId])  // 한 직원은 한 공지를 한 번만 읽음 처리
  @@index([announcementId])
  @@index([employeeId])
}
```

### 2. API 엔드포인트 (Express)

#### 바이어용 API (`/api/announcements`)
- **GET `/list`** - 공지사항 목록 조회 (읽음 통계 포함)
- **GET `/:id/readers`** - 공지사항 상세 조회 (읽은/안 읽은 직원 리스트)
- **POST `/create`** - 공지사항 작성
- **PUT `/:id`** - 공지사항 수정
- **DELETE `/:id`** - 공지사항 삭제

#### 직원용 API (`/api/announcements`)
- **GET `/my-announcements`** - 내 공지사항 조회 (읽음 여부 포함)
- **POST `/:id/read`** - 공지사항 읽음 처리

### 3. 바이어 대시보드 UI
**경로**: `/dashboard/announcements`

#### 기능
- 공지사항 목록 표시 (제목, 우선순위, 작성일시, 읽음 통계)
- 공지사항 작성 모달
  - 제목 입력
  - 내용 입력 (textarea)
  - 우선순위 선택 (긴급, 보통, 낮음)
- 공지사항 상세 모달
  - 공지 내용 표시
  - 읽은 직원 리스트 (이름, 읽은 시간)
  - 안 읽은 직원 리스트 (이름)
  - 읽음 통계 (총 인원, 읽음 인원, 읽음 비율)
- 공지사항 삭제 기능

#### UI 구성
```
📢 회사 공지사항 관리
┌─────────────────────────────────────────┐
│ [➕ 새 공지 작성]                         │
├─────────────────────────────────────────┤
│ 공지사항 목록                              │
│ ┌───────────────────────────────────┐   │
│ │ 🚨 [긴급] 공지 제목                  │   │
│ │ 2026-02-20 23:00                  │   │
│ │ 읽음: 5/10명 (50%)                 │   │
│ │ [상세보기] [삭제]                    │   │
│ └───────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

### 4. 직원 출퇴근 페이지 공지 표시
**경로**: `/employee/attendance`

#### 기능
- 페이지 상단에 활성 공지사항 표시
- 안 읽은 공지는 강조 표시
- 공지 내용 표시 (접기/펼치기)
- "✅ 읽음 확인" 버튼
- 읽음 확인 후 버튼 비활성화

#### UI 구성
```
⏰ 출퇴근 관리
┌─────────────────────────────────────────┐
│ 📢 회사 공지사항 (2건)                    │
├─────────────────────────────────────────┤
│ 🚨 [긴급] 공지 제목                        │
│ 작성일: 2026-02-20 23:00                │
│ 내용: 공지사항 내용...                     │
│ [✅ 읽음 확인] 또는 ✓ 읽음 완료           │
└─────────────────────────────────────────┘
```

## 🔄 데이터 흐름

### 공지 작성 흐름
1. 바이어가 `/dashboard/announcements`에서 "새 공지 작성" 클릭
2. 제목, 내용, 우선순위 입력
3. `POST /api/announcements/create` 호출
4. DB에 CompanyAnnouncement 생성 (buyerId, companyId 자동 설정)
5. 공지사항 목록 새로고침

### 공지 읽기 흐름
1. 직원이 `/employee/attendance` 접속
2. `GET /api/announcements/my-announcements` 호출
3. 현재 직원의 buyerId에 해당하는 활성 공지사항 조회
4. 각 공지에 대해 읽음 여부 표시 (readLogs에 기록 있는지 확인)
5. "읽음 확인" 버튼 클릭 시 `POST /api/announcements/:id/read` 호출
6. DB에 AnnouncementReadLog 생성
7. 버튼 상태 변경 (✓ 읽음 완료)

### 읽음 통계 조회 흐름
1. 바이어가 `/dashboard/announcements`에서 "상세보기" 클릭
2. `GET /api/announcements/:id/readers` 호출
3. 공지사항 정보 + 회사 전체 장애인 직원 조회
4. readLogs와 비교하여 읽은/안 읽은 직원 분류
5. 통계 계산 (총 인원, 읽음 인원, 비율)
6. 모달에 결과 표시

## 📊 데이터 구조

### API 응답 예시

#### 공지사항 목록 (바이어)
```json
{
  "announcements": [
    {
      "id": "cm...",
      "title": "긴급 공지",
      "content": "내용...",
      "priority": "URGENT",
      "isActive": true,
      "createdAt": "2026-02-20T14:00:00Z",
      "stats": {
        "totalEmployees": 10,
        "readCount": 5,
        "unreadCount": 5,
        "readPercentage": 50
      }
    }
  ]
}
```

#### 공지사항 상세 (바이어)
```json
{
  "announcement": { "id": "...", "title": "...", "content": "..." },
  "readEmployees": [
    { "id": "...", "name": "홍길동", "phone": "010-1001-0001", "readAt": "2026-02-20T14:30:00Z" }
  ],
  "unreadEmployees": [
    { "id": "...", "name": "김철수", "phone": "010-1001-0002" }
  ],
  "stats": {
    "total": 10,
    "read": 5,
    "unread": 5,
    "readPercentage": 50
  }
}
```

#### 내 공지사항 (직원)
```json
{
  "announcements": [
    {
      "id": "cm...",
      "title": "긴급 공지",
      "content": "내용...",
      "priority": "URGENT",
      "createdAt": "2026-02-20T14:00:00Z",
      "isRead": false,
      "readAt": null
    }
  ]
}
```

## 🔐 권한 관리

### 바이어 (BUYER)
- ✅ 공지사항 작성, 수정, 삭제
- ✅ 공지사항 목록 조회 (통계 포함)
- ✅ 읽은 직원 리스트 조회
- ❌ 공지사항 읽음 처리 (직원만 가능)

### 직원 (EMPLOYEE)
- ✅ 내 공지사항 조회
- ✅ 공지사항 읽음 처리
- ❌ 공지사항 작성, 수정, 삭제

## 🛠️ 기술 스택

### Backend
- **Framework**: Express.js (TypeScript)
- **Database**: Prisma + SQLite
- **Authentication**: JWT (requireAuth middleware)

### Frontend
- **Framework**: Next.js 14 (App Router)
- **Styling**: Tailwind CSS
- **State Management**: React useState, useEffect

### Database
- **ORM**: Prisma
- **DB**: SQLite (dev), PostgreSQL (production ready)
- **Migrations**: Prisma Migrate

## 📁 파일 구조

```
webapp/
├── apps/
│   ├── api/
│   │   ├── prisma/
│   │   │   ├── schema.prisma (CompanyAnnouncement, AnnouncementReadLog 추가)
│   │   │   └── migrations/
│   │   │       └── 20260220225659_add_company_announcements/
│   │   └── src/
│   │       ├── index.ts (announcements 라우트 등록)
│   │       └── routes/
│   │           └── announcements.ts (Express Router)
│   └── web/
│       └── src/
│           └── app/
│               ├── employee/
│               │   └── attendance/
│               │       └── page.tsx (공지사항 표시 + 읽음 확인)
│               └── dashboard/
│                   └── announcements/
│                       └── page.tsx (바이어 대시보드)
└── ecosystem.config.cjs
```

## 🧪 테스트 방법

### 1. 바이어 계정으로 테스트
```bash
# 1. 바이어 로그인
URL: https://3000-i9nss1cey8kihvk6alb7i-02b9cc79.sandbox.novita.ai/login
아이디: buyer01 (또는 기타 바이어 계정)
비밀번호: buyer123

# 2. 공지사항 페이지 접속
URL: https://3000-i9nss1cey8kihvk6alb7i-02b9cc79.sandbox.novita.ai/dashboard/announcements

# 3. 새 공지 작성
- "➕ 새 공지 작성" 버튼 클릭
- 제목, 내용, 우선순위 입력
- "작성" 버튼 클릭

# 4. 통계 확인
- 공지사항 목록에서 "상세보기" 클릭
- 읽은/안 읽은 직원 리스트 확인
```

### 2. 직원 계정으로 테스트
```bash
# 1. 직원 로그인
URL: https://3000-i9nss1cey8kihvk6alb7i-02b9cc79.sandbox.novita.ai/employee/login
전화번호: 010-1001-0001 (또는 기타 직원 계정)
비밀번호: employee123

# 2. 출퇴근 페이지 접속
URL: https://3000-i9nss1cey8kihvk6alb7i-02b9cc79.sandbox.novita.ai/employee/attendance

# 3. 공지사항 확인
- 페이지 상단에 공지사항 표시 확인
- "✅ 읽음 확인" 버튼 클릭
- 버튼 상태가 "✓ 읽음 완료"로 변경 확인

# 4. 바이어 대시보드에서 확인
- 바이어 계정으로 다시 로그인
- 공지사항 상세보기에서 읽은 직원 리스트에 추가된 것 확인
```

### 3. API 테스트 (curl)
```bash
# 바이어 토큰 얻기
TOKEN=$(curl -s -X POST http://localhost:4000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"buyer01","password":"buyer123"}' \
  | jq -r '.token')

# 공지사항 목록 조회
curl -H "Authorization: Bearer $TOKEN" \
  http://localhost:4000/api/announcements/list

# 공지사항 작성
curl -X POST \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title":"테스트 공지","content":"내용입니다","priority":"NORMAL"}' \
  http://localhost:4000/api/announcements/create

# 직원 토큰 얻기
EMP_TOKEN=$(curl -s -X POST http://localhost:4000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"010-1001-0001","password":"employee123"}' \
  | jq -r '.token')

# 내 공지사항 조회
curl -H "Authorization: Bearer $EMP_TOKEN" \
  http://localhost:4000/api/announcements/my-announcements

# 읽음 처리
curl -X POST \
  -H "Authorization: Bearer $EMP_TOKEN" \
  http://localhost:4000/api/announcements/:id/read
```

## 📝 주의사항

### 1. 인증 관련
- 모든 API는 JWT 인증 필요 (`requireAuth` middleware)
- 바이어와 직원 권한 분리 (role 체크)

### 2. 데이터 관계
- CompanyAnnouncement는 Company와 BuyerProfile에 연결
- AnnouncementReadLog는 중복 방지 (unique constraint)
- Cascade 삭제 설정 (공지 삭제 시 읽음 기록도 삭제)

### 3. 성능 고려
- 읽음 통계는 매번 계산 (향후 캐싱 가능)
- 직원 수가 많을 경우 페이지네이션 추가 권장

### 4. UI/UX
- 안 읽은 공지는 강조 표시 (배경색 변경)
- 읽음 처리 후 즉시 UI 업데이트
- 우선순위에 따른 색상 구분

## 🚀 향후 개선 사항

1. **알림 기능**
   - 새 공지 등록 시 직원에게 푸시 알림
   - 이메일/SMS 알림 옵션

2. **공지 예약**
   - 특정 날짜/시간에 공지 자동 게시

3. **첨부 파일**
   - 공지에 PDF, 이미지 첨부 기능

4. **댓글/피드백**
   - 직원이 공지에 댓글/질문 가능

5. **통계 대시보드**
   - 월별 공지 통계
   - 읽음률 트렌드 분석

6. **공지 카테고리**
   - 업무, 복지, 안전, 기타 등 카테고리 분류

7. **다국어 지원**
   - 한국어/영어 등 다국어 공지

## 📞 관련 문의
- **프로젝트명**: 장표사닷컴 (장애인표준사업장 연계고용 플랫폼)
- **서비스 URL**: https://3000-i9nss1cey8kihvk6alb7i-02b9cc79.sandbox.novita.ai
- **Git Repository**: /home/user/webapp

## ✅ 구현 완료 체크리스트

- [x] 데이터베이스 스키마 설계 (CompanyAnnouncement, AnnouncementReadLog)
- [x] Prisma 마이그레이션 생성 및 적용
- [x] API 라우트 구현 (바이어용 6개, 직원용 2개)
- [x] 바이어 대시보드 UI 구현
- [x] 직원 출퇴근 페이지 공지 표시 구현
- [x] 읽음 확인 버튼 구현
- [x] 읽은 직원 리스트 표시 구현
- [x] 권한 관리 (바이어/직원 분리)
- [x] API 테스트 (Express Router)
- [x] Git 커밋
- [x] 문서화

---

**구현 완료일**: 2026년 2월 20일 23:30 (KST)
**최종 테스트**: API 서버 정상 작동 확인 (http://localhost:4000/health)
**Git Commit**: 38915fa (회사 공지사항 API 라우트 Express로 수정 완료)
